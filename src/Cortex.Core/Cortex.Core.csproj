<Project Sdk="Microsoft.NET.Sdk">

  <!-- Pure domain layer: entities, value objects, abstractions. No external dependencies. -->
  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <PackageId>Cortex.Core</PackageId>
    <Description>Core domain primitives for the Cortex AI platform — entities, multi-tenancy interfaces, result types.</Description>
  </PropertyGroup>

</Project>
